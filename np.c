#include "monty.h"

/**
 * nopnode - function which does nothing
 * @head:head of stack
 * @num:number line
 * Return:nothing
*/
void nopnode(stack_t **head, unsigned int num)
{
	(void)head;
	(void)num;
}
